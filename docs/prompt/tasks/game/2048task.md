````md
# Prompt: Generate a Fully Runnable Next.js + Phaser 2048 Game Repository

You are an expert full-stack game engineer.  
Generate a **complete, production-ready, runnable GitHub repository** for a 2048 game built with **Phaser 3 inside a Next.js (App Router) project**.

The output must be **fully copy-pastable and runnable with no missing files or pseudocode**.

---

# 🎯 1. Goal

Build a working **2048 game** with:

- Phaser 3 for game rendering and logic
- Next.js App Router for hosting the game page
- TypeScript support
- Browser-based gameplay

The project must run with:

```bash
npm install
npm run dev
```
````

---

# ⚙️ 2. Tech Stack

- Next.js (App Router, React 18+)
- TypeScript
- Phaser 3 (latest stable version)
- Node.js environment

---

# 📁 3. Required Project Structure

You MUST generate the following structure exactly:

```
src/
  app/
    (game)/
      2048/
        page.tsx

  game/
    phaserGame.ts
    config.ts
    scenes/
      GameScene.ts
      UIScene.ts
```

---

# 🎮 4. Game Requirements (2048 Rules)

Implement full 2048 gameplay:

## Core Mechanics

- 4x4 grid
- Tiles slide in four directions
- Merge identical tiles once per move
- New tile spawn after each move:
  - 90% probability → 2
  - 10% probability → 4

## Controls

- Arrow keys:
  - ↑ ↓ ← →

- Restart functionality

## Win/Loss Conditions

- Win: reach 2048 tile
- Lose: no valid moves remain

---

# 🧠 5. Phaser Integration Rules (Critical)

- Phaser MUST ONLY run on client-side
- No SSR execution allowed
- Use `useEffect` or dynamic import for initialization

### Required behavior:

- Phaser instance must be safely destroyed on unmount:
  - `game.destroy(true)`

- Prevent React Strict Mode double initialization

---

# ⚛️ 6. Next.js Requirements

### page.tsx rules:

- Must include `"use client"`
- Must NOT directly import Phaser at top level
- Must only mount a container:

```html
<div id="game-container"></div>
```

- Game initialization must happen inside `useEffect`

---

# 🧩 7. Code Quality Requirements

- Clean modular architecture
- No monolithic files
- Game logic separated from rendering
- Scenes must be used properly:
  - GameScene → logic + rendering
  - UIScene → score + UI overlay

---

# 🎨 8. UI Requirements

Use Phaser rendering:

- Colored tiles based on value
- Smooth movement transitions (basic tween optional)
- Score display
- Game Over / Win overlay

---

# 📦 9. Dependency Constraints

Allowed dependencies:

- next
- react
- phaser
- typescript (optional)

❌ Do NOT use:

- pixi.js
- three.js
- external state management libraries
- unnecessary UI frameworks

---

# 🧱 10. Output Format (VERY IMPORTANT)

You MUST output in this exact order:

---

## 1. Project Structure Tree

Show full file tree.

---

## 2. Full Source Code (File by File)

For each file:

- Show file path as heading
- Provide complete code block
- No omissions, no pseudocode

---

## 3. Installation & Run Instructions

```bash
npm install
npm run dev
```

---

## 4. Important Notes

Explain briefly:

- Why Phaser must be client-only in Next.js App Router
- How SSR issues are avoided
- How lifecycle cleanup works

---

# 🚀 11. Optional Enhancements (Only if time allows)

If not increasing complexity too much:

- Tile merge animation
- Slide animation using Phaser tweens
- Smooth score update animation
- Restart button UI

---

# 🧠 Core Constraint Summary

This must be:

> A fully working, directly runnable Next.js + Phaser 2048 project repository.

It must NOT be:

- partial code
- pseudocode
- missing files
- SSR-broken Phaser usage
- incomplete game logic

```

```
